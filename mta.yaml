_schema-version: 3.3.0
ID: adapt-ui
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: adapt-ui-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: adapt-ui-auth
      - name: adapt-ui-db
      - name: adapt-ui-destination

  - name: adapt-ui-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: adapt-ui-db

  - name: adapt-ui-app-deployer
    type: com.sap.application.content
    path: gen
    requires:
      - name: adapt-ui-html5-repo-host
        parameters:
          content-target: true
    build-parameters:
      build-result: app/
      requires:
        - name: adaptuiadaptui
          artifacts:
            - adaptui.zip
          target-path: app/

  - name: adaptuiadaptui
    type: html5
    path: app/adaptui
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: adapt-ui-destinations
    type: com.sap.application.content
    requires:
      - name: adapt-ui-auth
        parameters:
          service-key:
            name: adapt-ui-auth-key
      - name: adapt-ui-html5-repo-host
        parameters:
          service-key:
            name: adapt-ui-html5-repo-host-key
      - name: srv-api
      - name: adapt-ui-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: adapt-ui-html5-repository
              ServiceInstanceName: adapt-ui-html5-repo-host
              ServiceKeyName: adapt-ui-html5-repo-host-key
              sap.cloud.service: adaptui.service
            - Name: adapt-ui-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: adapt-ui-auth
              ServiceKeyName: adapt-ui-auth-key
              sap.cloud.service: adaptui.service

resources:
  - name: adapt-ui-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: adapt-ui-${org}-${space}
        tenant-mode: dedicated
  - name: adapt-ui-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: adapt-ui-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true

  - name: adapt-ui-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
  - name: adapt-ui-html5-runtime
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-runtime
